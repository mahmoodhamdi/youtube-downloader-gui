# خطة تنظيف الأركيتكتشر (Architecture Cleanup Plan)

## نظرة عامة

هذه الخطة لتنظيف المشروع من الملفات القديمة والانتقال بالكامل للأركيتكتشر الجديد v2.0.

---

## الوضع الحالي

### الملفات الموجودة:
| الملف | الحجم | الغرض | الحالة |
|-------|-------|-------|--------|
| `youtube_downloader.py` | ~1200 سطر | النسخة الأصلية (single-class) | **قديم - للحذف** |
| `youtube_downloader1.py` | ~1960 سطر | النسخة المحسنة (tabbed interface) | **قديم - للحذف** |
| `main.py` | ~63 سطر | Entry point للأركيتكتشر الجديد | **الحالي** |
| `src/` | - | الأركيتكتشر الجديد v2.0 | **الحالي** |

### المشكلة:
- وجود 3 طرق مختلفة لتشغيل التطبيق
- تضارب في الأكواد والوظائف
- صعوبة الصيانة والتطوير

---

## خطة التنفيذ

### الخطوة 1: حذف الملفات القديمة
```
حذف: youtube_downloader.py
حذف: youtube_downloader1.py
```

### الخطوة 2: تحديث CLAUDE.md
- إزالة الإشارات للملفات القديمة
- التأكيد على `main.py` كنقطة الدخول الوحيدة

### الخطوة 3: تحديث README.md
- تحديث تعليمات التشغيل
- إزالة الإشارات للملفات القديمة

---

## الأركيتكتشر بعد التنظيف

```
youtube-downloader-gui/
├── main.py                      # 🟢 Entry point الوحيد
├── requirements.txt
├── README.md
├── CLAUDE.md
├── DEVELOPMENT_PLAN.md
│
├── src/                         # 🟢 كل الكود هنا
│   ├── __init__.py
│   │
│   ├── core/                    # Business Logic
│   │   ├── __init__.py
│   │   ├── queue_manager.py     # إدارة الـ Queue
│   │   └── download_manager.py  # التحميل مع yt-dlp
│   │
│   ├── config/                  # الإعدادات
│   │   ├── __init__.py
│   │   ├── config_manager.py
│   │   ├── validators.py
│   │   └── defaults.py
│   │
│   ├── ui/                      # واجهة المستخدم
│   │   ├── __init__.py
│   │   ├── main_window.py       # النافذة الرئيسية
│   │   ├── tabs/
│   │   ├── widgets/
│   │   ├── themes/
│   │   └── dialogs/
│   │
│   ├── utils/                   # أدوات مساعدة
│   │   ├── __init__.py
│   │   ├── logger.py
│   │   ├── error_handler.py
│   │   ├── cache.py
│   │   └── file_utils.py
│   │
│   └── exceptions/              # الأخطاء المخصصة
│       ├── __init__.py
│       └── errors.py
│
├── tests/                       # الاختبارات
├── resources/                   # الموارد
└── plans/                       # خطط التطوير
```

---

## طريقة التشغيل بعد التنفيذ

### تشغيل التطبيق:
```bash
python main.py
```

### تدفق البيانات:
```
main.py
    └── src/ui/main_window.py (MainWindow)
            ├── src/config/config_manager.py (ConfigManager)
            ├── src/core/queue_manager.py (QueueManager)
            ├── src/core/download_manager.py (DownloadManager)
            ├── src/ui/tabs/ (Downloads, Settings, History)
            └── src/utils/logger.py (Logger)
```

---

## الملفات المتأثرة

| الملف | الإجراء |
|-------|---------|
| `youtube_downloader.py` | حذف |
| `youtube_downloader1.py` | حذف |
| `CLAUDE.md` | تحديث (تم بالفعل) |
| `README.md` | تحديث |

---

## التحقق بعد التنفيذ

1. ✅ `python main.py` يعمل بدون أخطاء
2. ✅ لا توجد ملفات قديمة في المشروع
3. ✅ التوثيق محدث
4. ✅ Git commit نظيف

---

## ملاحظات

- الأركيتكتشر الجديد v2.0 مكتمل ويعمل
- الملفات القديمة كانت للتطوير التدريجي فقط
- لا يوجد فقدان لأي وظائف - كل شيء موجود في `src/`
